version: '2.1'

volumes:
  storage-data:
  tailscale:

services:
  samba:
    build: samba
    container_name: samba
    restart: always
    privileged: true
    ports:
      - "139:139"
      - "445:445"
    devices:
      - /dev/sda1:/dev/sda1
    labels:
      io.balena.features.dbus: '1'
    volumes:
      - storage-data:/dev/sda1
      - /sys:/sys
    environment:
      - UDEV=1

  tailscale:
    build: tailscale
    restart: on-failure
    cap_add:
      - NET_ADMIN
      - NET_RAW
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_AUTHKEY=${TS_AUTHKEY}
<<<<<<< HEAD
      - TS_USERSPACE=false
    tmpfs:
=======
      - TS_EXTRA_ARGS=--accept-dns=false
    labels:
      io.balena.features.kernel-modules: 1
    tmpfs:
      - /tmp
>>>>>>> e6f956986f6bc4a7a4bab230e6a61aa31d50cc99
      - /run
    volumes:
      - tailscale:/var/lib/tailscale